require 'spec_helper'

describe Appiconset do
  it 'has a version number' do
    expect(Appiconset::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
